var React = require('react');
var Header = React.createClass({
  render: function() {
    return (
      <h1 className="header">
        My Web App
      </h1>
    );
  }
});

module.exports = Header;